create table if not exists affiliate_links (id uuid primary key default gen_random_uuid(), event_id uuid references events(id) on delete cascade, trip_id uuid references trips(trip_id) on delete cascade, provider text not null, url text not null, label text, created_at timestamptz default now()); create table if not exists event_venues (event_id uuid primary key references events(id) on delete cascade, name text, lat double precision, lon double precision, radius_m int default 2000, updated_at timestamptz default now());