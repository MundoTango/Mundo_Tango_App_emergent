import { describe,it,expect } from 'vitest'; import { providers } from '../../src/lib/affiliateProviders'; describe('affiliate providers',()=>{ it('booking URL contains AID',()=>{ process.env.BOOKING_AID='aid_test'; const url=providers.booking.hotelUrl({city:'Athens',checkin:'2026-06-12',checkout:'2026-06-15'}); expect(url).toContain('aid=aid_test'); expect(url).toContain('Athens'); }); });