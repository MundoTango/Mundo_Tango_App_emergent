import { trackAffiliateClick } from '@/src/lib/plausible'; export function TripCard({trip,links}:{trip:any,links:any[]}){return (<div className='rounded-xl border p-4 shadow-sm'><div className='flex items-center justify-between'><div><h3 className='text-lg font-semibold'>{trip.destination_city}</h3><p className='text-sm opacity-80'>{trip.start_date} → {trip.end_date} · {trip.travel_status}</p></div></div><div className='mt-3 flex flex-wrap gap-2'>{links.map((l:any)=>(<a key={l.id} href={l.url} target='_blank' rel='noopener noreferrer' onClick={()=>trackAffiliateClick(l.provider,trip.destination_city,trip.event_id)} className='rounded-md px-3 py-2 text-sm font-medium ring-1 ring-gray-300 hover:bg-gray-50'>{l.label}</a>))}</div></div>);}