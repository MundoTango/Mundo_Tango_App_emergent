/**
 * usePageAgent Hook
 * MB.MD Created: October 19, 2025 (Persistent version)
 */

import { usePageAgent as usePageAgentContext } from '@/contexts/PageAgentContext';

export const usePageAgent = usePageAgentContext;
